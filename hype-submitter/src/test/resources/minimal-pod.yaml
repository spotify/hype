apiVersion: v1
kind: Pod

spec:
  restartPolicy: Never

  containers:
  - name: hype-run
    image: us.gcr.io/my-project/hype-runner:7
    imagePullPolicy: Always

    env:
    - name: EXAMPLE
      value: my-env-value

    resources:
      requests:
        cpu: 100m
      limits:
        memory: 1Gi
